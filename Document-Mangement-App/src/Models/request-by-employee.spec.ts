import { RequestByEmployee } from './request-by-employee';

describe('RequestByEmployee', () => {
  it('should create an instance', () => {
    expect(new RequestByEmployee()).toBeTruthy();
  });
});
